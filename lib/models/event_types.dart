/* Declare a set of types for events and event updates. This enum ensures a
uniform system of checking activity type */
enum EventTypes {
  football,
  volleyball,
  basketball,
  wrestling,
  track,
  golf,
  school,
  other,
}
